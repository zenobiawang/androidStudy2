<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="ViewGroupDemo">
        <attr name="count" format="integer"/>
    </declare-styleable>

    <declare-styleable name="ViewDemo">
        <attr name="textStartColor" format="color"/>
        <attr name="textEndColor" format="color"/>
        <attr name="textSize" format="dimension"/>
        <attr name="text" format="string"/>
    </declare-styleable>
    
    <declare-styleable name="SoundViewDemo">
        <attr name="radius" format="integer"/>
        <attr name="blockHeight" format="dimension"/>
        <attr name="blockSpacing" format="dimension"/>
        <attr name="angle" format="integer"/>
        <attr name="defaultColor" format="color"/>
        <attr name="currentColor" format="color"/>
        <attr name="centerSrc" format="reference"/> 
    </declare-styleable>

</resources>
